export default postModule = {
  state: () => ({
    posts: [],
    show: false,
    isLoading: false,
    isLoadingPage: false,
    page: 1,
    limit: 5,
    totalPages: 1,
    searchQuery: '',
    selectedSort: '',
    sortOptions: [
      { value: 'title', name: 'Name' },
      { value: 'body', name: 'Description' },
    ],
  }),
  getters: {},
  mutations: {
    setPosts(state, posts) {
      state.posts = posts;
    },
    setPosts(state, posts) {
      state.posts = posts;
    },
    setPosts(state, posts) {
      state.posts = posts;
    },
    setPosts(state, posts) {
      state.posts = posts;
    },
    setPosts(state, posts) {
      state.posts = posts;
    },
    setPosts(state, posts) {
      state.posts = posts;
    },
    setPosts(state, posts) {
      state.posts = posts;
    },
    setPosts(state, posts) {
      state.posts = posts;
    },
    setPosts(state, posts) {
      state.posts = posts;
    },

    setPosts(state, posts) {
      state.posts = posts;
    },
  },
  actions: {},
};
